# Local dev: Vite proxies /api (see vite.config.js). Optional: VITE_API_PROXY_TARGET=http://127.0.0.1:5006
#
# Production (Vercel): either rely on frontend/vercel.json rewrites to your Render URL (no CORS),
# or set VITE_API_BASE=https://your-service.onrender.com (Render must allow your Vercel origin in CORS).
# VITE_API_BASE=https://grubpac-satyam.onrender.com
